import { DateToUtcPipe } from './date-to-utc.pipe';

describe('DateToUtcPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToUtcPipe();
    expect(pipe).toBeTruthy();
  });
});
